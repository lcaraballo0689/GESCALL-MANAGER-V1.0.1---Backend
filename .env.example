# Backend Server Configuration
# Port on which the Node.js server will run
PORT=3001
# Environment: development or production
NODE_ENV=development

# Database Configuration (MySQL / Vicidial Database)
# Connects directly to the Asterisk/Vicidial database
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=cron
DB_PASSWORD=1234
DB_NAME=asterisk

# Vicidial API Configuration
# Used for non-agent actions like status updates
VICIDIAL_API_URL=http://localhost/vicidial/non_agent_api.php
VICIDIAL_API_USER=6666
VICIDIAL_API_PASS=1234
VICIDIAL_SOURCE=admin_panel

# CORS Configuration
# Allowed origin for frontend requests. Use * for all or https://yourdomain.com
CORS_ORIGIN=*

# SSH Configuration for Vicidial Server (Audio Management)
# Required for uploading, converting, and managing audio files via SFTP/SSH
VICIDIAL_SSH_HOST=127.0.0.1
VICIDIAL_SSH_USER=root
# SSH Password (consider using keys in production if supported by implementation, currently password based)
VICIDIAL_SSH_PASSWORD=secret
# Path where sounds are stored in Asterisk server (standard: /var/lib/asterisk/sounds)
VICIDIAL_SOUNDS_PATH=/var/lib/asterisk/sounds
